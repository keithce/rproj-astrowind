# Implementation Plans

Generated by the improve skill on 2026-07-19, against commit `43a2df2`. The session ran
non-interactively, so per the skill's default the top 5 findings by leverage were planned;
everything else from the audit is recorded in the backlog below so a future run (or the
maintainer) can promote items to plans without re-auditing.

Note: at planning time the working tree had uncommitted modifications to
`src/assets/styles/starwind.css` and `src/components/common/BasicScripts.astro` (unrelated
to any plan's scope).

Execute in the order below unless dependencies say otherwise. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                                                                   | Priority | Effort | Depends on | Status |
| ---- | ----------------------------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Make CI run the test suites on every PR                                 | P1       | S      | —          | TODO   |
| 002  | Fix agent-facing doc drift + add .env.example                           | P1       | S      | —          | TODO   |
| 003  | Contact action: tests + fix silent email failure & duplicate-lead retry | P1       | M      | 001        | TODO   |
| 004  | Put bot protection in front of the contact action                       | P1       | M      | 003        | TODO   |
| 005  | Make dependency overrides effective under Bun; remove dead deps         | P1       | M      | 001 (soft) | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 003 requires 001 because its new unit tests only gate PRs once CI runs `test:unit`.
- 004 requires 003 because the bot check is added to the thin action wrapper that 003 creates; doing 004 first would double the churn in `src/actions/index.ts`.
- 005 soft-depends on 001: the E2E suite is 005's regression gate for lockfile churn; run 001 first so the same gate exists in CI.
- 002 is independent — do it any time, but early, since executors of 003–005 read `CLAUDE.md`.

## Audit backlog (vetted findings not yet planned)

Highest-leverage first. IDs reference the 2026-07-19 audit; evidence was verified by the advisor unless marked "investigate".

| ID               | Finding                                                                                                                                                                                                                                                                | Effort | Notes                                                                                                     |
| ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------- |
| SEC-02           | No site-wide security headers (only per-path X-Frame-Options in `vercel.json`; no CSP/nosniff/HSTS; no Astro middleware)                                                                                                                                               | S      | Next candidate to plan. CSP should start report-only — Partytown/Cal.com/analytics will need allowlisting |
| CORRECT-02       | `src/loaders/frequencyEssayLoader.ts:86` and `githubEditorialLoader.ts` swallow ALL manifest errors (incl. JSON/contract violations) as "remote fetch failed" — corrupt published manifest is indistinguishable from offline                                           | S      | Pairs with DEBT-01                                                                                        |
| DEBT-01          | ~90% code duplication between `frequencyEssayLoader.ts` and `githubEditorialLoader.ts` (~130 lines); already drifted once (only editorial clears its store)                                                                                                            | M      | Extract shared `githubManifestLoader(config)`; fix CORRECT-01/02 once in the shared code                  |
| CORRECT-01       | `frequencyEssayLoader` never prunes deleted essays (no `store.delete`); deleted essays persist across cached builds. Needs targeted diff-and-delete because it shares the store with the glob loader via `mergeLoaders`                                                | M      | Fold into the DEBT-01 refactor                                                                            |
| CORRECT-04       | Vendored loader rate-limit backoff keys on `msg.includes('rate limited')` substring (`vendor/notion-astro-loader/src/loader.ts:213,288`) instead of `code === 'rate_limited'`/HTTP 429 — real 429s can abort the whole sync                                            | S      |                                                                                                           |
| CORRECT-05       | Render failures in vendored loader silently drop pages (no `store.set`, no prune, digest never advances → re-fails every build) `loader.ts:286-297`                                                                                                                    | M      |                                                                                                           |
| PERF-01          | Full resource dataset JSON-inlined into every paginated/category/type page (`src/pages/resources/all/[page].astro:28-30` + 3 siblings; same pattern on TIL pages). Code's own TODO says "replace with a static JSON endpoint"                                          | M      | Also applies to `til/[tag]/[page].astro`, `til/all/[page].astro`                                          |
| SEC-03           | Notion HTML rendered via `set:html` with no real sanitizer — `rehypeSanitizeRecursive` is structural normalization only (verified). Single-author CMS today, so exploitability is low; becomes stored-XSS if any externally-influenced Notion content is ever rendered | M      | Add `rehype-sanitize` allowlist in `vendor/notion-astro-loader/src/render.ts`; rename the misleading pass |
| DEBT-05 / DIR-02 | AstroWind template leftovers still live+indexable: `src/pages/homes/*` (4 pages), `src/pages/landing/*` (6 pages), commented nav refs `src/navigation.ts:41-61`; also dead `src/utils/env-loader.ts`                                                                   | S      | Contradicts the brand's "less template" goal; delete after checking external links                        |
| TEST-03          | E2E runs only against `astro dev`, where the production Notion loader is disabled (`isDevServer` fallback in `src/content.config.ts`) — shipped artifact never tested                                                                                                  | M      | Add a build+preview Playwright project on top of plan 001                                                 |
| TEST-04          | Loader cache/merge, legacy-slug redirect dedup (`resources/[slug].astro:45-63`), and pagination clamping have zero characterization tests                                                                                                                              | M      | Natural follow-on once 003 establishes the unit-test pattern                                              |
| DEBT-03          | Resource filter/normalization logic reimplemented ~6× across `resources.ts`, `resource-serialization.ts`; canonical `string-array.ts` unused by them                                                                                                                   | M      |                                                                                                           |
| DEBT-04          | Dead `getLiveCollection` fallback in `src/utils/resources.ts:32-65` — no live collection is registered anywhere; branch can only throw                                                                                                                                 | S      |                                                                                                           |
| DEBT-02          | Notion data-source resolution duplicated (`src/utils/notion-datasource.ts` vs inline in vendored loader) with divergent caching                                                                                                                                        | S      |                                                                                                           |
| DEBT-06          | `any`/`@ts-ignore` cluster around Notion 2025-09-03 API in vendored loader — the whole multi-source query path is untyped                                                                                                                                              | M      |                                                                                                           |
| PERF-02          | Notion image downloads sequential per page at build time (`vendor/notion-astro-loader/src/render.ts`)                                                                                                                                                                  | M      | Bounded-concurrency pool                                                                                  |
| PERF-03          | Three variable font families imported globally (`src/assets/styles/theme.css:1-3`)                                                                                                                                                                                     | S–M    | Measure first                                                                                             |
| PERF-04          | `astro-compress` double-minifies what Vite + edge compression already handle                                                                                                                                                                                           | S      | Measure before/after                                                                                      |
| PERF-05          | Contact action re-resolves Notion data source per cold start                                                                                                                                                                                                           | S      | Optional env pin                                                                                          |
| CORRECT-06       | `blog.ts:277` tolerates undefined tag slugs (`/tag/undefined` route possible); side-effect `.some(push)` lookups                                                                                                                                                       | S      |                                                                                                           |
| SEC-01b          | Per-IP rate limiting as second layer behind BotID                                                                                                                                                                                                                      | M      | Blocked on identifying the `REDIS_URL` provider                                                           |
| SEC-05           | Disposable-email/content validation beyond the 3-word denylist                                                                                                                                                                                                         | S      | Only after 004                                                                                            |
| DEP-03           | Vendored notion-astro-loader fork drift vs upstream unverified                                                                                                                                                                                                         | M      | Investigate: record base commit, diff upstream                                                            |
| DX-02            | `dev` script hardwires `DEBUG_NOTION_LOADER=1`                                                                                                                                                                                                                         | S      |                                                                                                           |
| DX-03            | No pre-commit hooks despite full lint/format tooling                                                                                                                                                                                                                   | S      |                                                                                                           |
| DX-04            | 3 orphaned scripts in `scripts/` (generate-design-images, generate-rhythm-images, html-compare)                                                                                                                                                                        | S      | May be manual one-offs — ask maintainer                                                                   |
| DOCS-04          | One-off migration reports intermixed with living docs in `docs/`                                                                                                                                                                                                       | S      | Move to `docs/history/`                                                                                   |

## Direction findings (maintainer decisions, not defects)

Presented for the maintainer to weigh — grounded in repo evidence, but strategy calls:

- **DIR-01 — Orphaned Writing/editorial section**: nav link removed (`7dc068b`) but `src/pages/writing/*` routes, the `editorial` collection, and `/writing` internal-link references still ship; the index renders an empty state. Decide: finish (publish Frequency snapshots, restore nav) or remove the whole surface. Effort: M to finish / S to remove.
- **DIR-03 — Photography portfolio gap**: README + CLAUDE.md sell "photography-forward" finishing services, but no gallery/portfolio page exists and no photography service page (services are Design/Rhythm/Color/Motion). Cloudinary infra already present (`src/utils/cloudinary.ts`). Highest-value direction item for the site's evaluative audience. Effort: M (design spike first).
- **DIR-04 — Newsletter signup stated-but-undelivered**: README claims newsletter signup; only past-issue posts exist. Resend + welcome-email infra already built, making this disproportionately cheap. Effort: S–M (or delete the README claim).
- **DIR-05 — Abstract service names vs what buyers search for**: Design/Rhythm/Color/Motion don't say "mixing & mastering / film finishing" to an evaluative visitor. Pairing each name with a concrete descriptor in nav/meta is an S-effort copy change — but naming may be a deliberate brand choice; maintainer's call.

## Findings considered and rejected

- Committed secrets: none — `.env` is gitignored and absent from all git history; no hardcoded tokens in tracked files. Not a finding.
- Committed build artifacts (`dist/`, `playwright-report/`, `test-results/`, `tsconfig.tsbuildinfo`): all correctly gitignored and untracked; they only exist on local disk. Not a finding.
- `https_proxy`-style platform conventions, GA measurement ID in `src/config.yaml`: public-by-design values, not secrets.
- Existing Playwright spec quality: reviewed and found good (web-first assertions, no arbitrary waits) — breadth, not quality, is the gap (covered by 001/TEST-03/TEST-04).
- SEC-04 (submitter name in `console.debug`): folded into plan 003 (the log line is rewritten there), not a standalone plan.
- DX-01 (.env.example missing): folded into plan 002.
- DX-05 / DEP-01 (pnpm/pbun override keys): folded into plan 005.
- PERF-06 (unused notion-client/notion-utils): folded into plan 005.
