---
import FormContainer from '~/components/ui/Form.astro';
import Headline from '~/components/ui/Headline.astro';
import WidgetWrapper from '~/components/ui/WidgetWrapper.astro';
import type { Contact as Props } from '~/types';
import { BotIdClient } from 'botid/client';

const {
  title = await Astro.slots.render('title'),
  subtitle = await Astro.slots.render('subtitle'),
  tagline = await Astro.slots.render('tagline'),
  inputs,
  textarea,
  disclaimer,
  button,
  description,
  formAction,

  id,
  isDark = false,
  classes = {},
  bg = await Astro.slots.render('bg'),
} = Astro.props;

const protectedRoutes = [
  {
    path: '/api/submit-to-notion',
    method: 'POST',
  },
] satisfies Array<{ path: string; method: string }>;
---

<WidgetWrapper id={id} isDark={isDark} containerClass={`max-w-7xl mx-auto ${classes?.container ?? ''}`} bg={bg}>
  <Headline title={title} subtitle={subtitle} tagline={tagline} />

  {
    inputs && (
      <div class="flex flex-col p-4 mx-auto w-full max-w-xl rounded-lg border shadow-md backdrop-blur-sm border-accent bg-accent/15 sm:p-6 lg:p-8">
        <FormContainer
          inputs={inputs}
          textarea={textarea}
          disclaimer={disclaimer}
          button={button}
          description={description}
          formAction={formAction}
        />
      </div>
    )
  }
  <BotIdClient protect={protectedRoutes} />
</WidgetWrapper>
