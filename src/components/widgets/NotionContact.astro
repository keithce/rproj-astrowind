---
import type { Input } from '~/types';
import Contact from './Contact.astro';

const { defaultService = '' } = Astro.props;

const inputs: Array<Input> = [
  {
    name: 'name',
    label: 'Name',
    type: 'text',
    required: true,
    placeholder: 'Your name',
  },
  {
    name: 'email',
    label: 'Email',
    type: 'email',
    required: true,
    placeholder: 'you@email.com',
  },
  {
    name: 'service',
    label: 'Service',
    type: 'select',
    required: true,
    options: [
      { value: '', label: 'Select a service' },
      { value: 'Design', label: 'Design' },
      { value: 'Rhythm', label: 'Rhythm' },
      { value: 'Color', label: 'Color' },
      { value: 'Motion', label: 'Motion' },
    ],
    defaultValue: defaultService,
  },
];

const textarea = {
  name: 'message',
  label: 'Message',
  required: true,
  placeholder: 'Share your ideas, questions, or goals with us!',
};

const button = 'Start the Conversation';

// const description = "We love connecting with creative minds! Reach out and let's make something amazing together."; // Unused variable
---

<Contact
  title="Ready to Start Your Project?"
  subtitle="Tell us about your vision. We'll respond within 24 hours with next steps."
  inputs={inputs}
  textarea={textarea}
  button={button}
  description="Share your goals and we'll show you exactly how we can help. No pressure, just honest advice."
  formAction="/api/submit-to-notion"
  {...Astro.props}
/>

<!-- Progressive Enhancement: Form works without JS, enhanced with JS -->
<noscript>
  <style>
    /* Hide JS-only UI elements when JavaScript is disabled */
    .js-only {
      display: none !important;
    }
  </style>
</noscript>

<script is:inline src="/form-handler.js" type="module"></script>

<script>
  // Progressive enhancement marker - signals that JS is available
  document.documentElement.classList.add('js-enabled');

  // Verify form handler integration
  document.addEventListener('DOMContentLoaded', () => {
    const form = document.getElementById('notion-contact-form');
    if (form) {
      // Add indicator that JavaScript enhancements are available
      form.setAttribute('data-js-enhanced', 'true');
    }
  });
</script>
