---
import { Icon } from 'astro-icon/components';

import Image from '~/components/common/Image.astro';
import PostTags from '~/components/blog/Tags.astro';
import SocialShare from '~/components/common/SocialShare.astro';
import RelatedPages from '~/components/ui/RelatedPages.astro';
import ContextualLinks from '~/components/ui/ContextualLinks.astro';
import { getRelatedPages /* , generateLinkSuggestions */ } from '~/utils/internal-linking';

import { getPermalink } from '~/utils/permalinks';
import { getFormattedDate } from '~/utils/utils';

import type { Post } from '~/types';

export interface Props {
  post: Post;
  url: string | URL;
}

export const prerender = true;
const { post, url } = Astro.props;

// Generate contextual links based on post content
// const postContent = typeof post.content === 'string' ? post.content : ''; // Unused variable
// const linkSuggestions = generateLinkSuggestions(postContent, Astro.url.pathname); // Unused variable
const relatedPages = getRelatedPages(Astro.url.pathname, 3, true);

// Convert link suggestions to contextual links format
// const contextualLinks = linkSuggestions.slice(0, 3).map(suggestion => {
//   const pageData = Object.values(import('~/utils/internal-linking').then(m => m.SITE_STRUCTURE));
//   return {
//     text: `Explore ${suggestion.anchor}`,
//     href: suggestion.href,
//     icon: 'tabler:arrow-right'
//   };
// });

// Service-specific links based on post tags/category
const serviceLinks = [
  {
    text: 'Music Production Services',
    href: '/services/rhythm',
    icon: 'tabler:music',
    description: 'Professional audio mixing and mastering',
  },
  {
    text: 'Creative Technology Consulting',
    href: '/services/design',
    icon: 'tabler:settings',
    description: 'Workflow optimization and tech solutions',
  },
  {
    text: 'Photography & Color Work',
    href: '/services/color',
    icon: 'tabler:camera',
    description: 'Visual storytelling and photography services',
  },
];
---

<section class="mx-auto py-8 sm:py-16 lg:py-20">
  <article data-pagefind-body>
    <header class={post.image ? 'intersect-once intersect-quarter' : 'intersect-once intersect-quarter'}>
      <div class="mx-auto mt-0 mb-2 flex max-w-3xl flex-col justify-between px-4 sm:flex-row sm:items-center sm:px-6">
        <p>
          <Icon name="tabler:clock" class="text-muted-foreground -mt-0.5 inline-block h-4 w-4" />
          <time datetime={String(post.publishDate)} class="inline-block">{getFormattedDate(post.publishDate)}</time>
          {
            post.author && (
              <>
                {' '}
                · <Icon name="tabler:user" class="text-muted-foreground -mt-0.5 inline-block h-4 w-4" />
                <span class="inline-block">{post.author}</span>
              </>
            )
          }
          {
            post.category && (
              <>
                {' '}
                ·{' '}
                <a class="inline-block hover:underline" href={getPermalink(post.category.slug, 'category')}>
                  {post.category.title}
                </a>
              </>
            )
          }
          {
            post.readingTime && (
              <>
                &nbsp;· <span>{post.readingTime}</span> min read
              </>
            )
          }
        </p>
      </div>

      <h1
        class="leading-tighter font-heading mx-auto max-w-3xl px-4 text-4xl font-bold tracking-tighter sm:px-6 md:text-5xl"
      >
        {post.title}
      </h1>
      <p class="text-muted-foreground mx-auto mt-4 mb-8 max-w-3xl px-4 text-justify text-xl sm:px-6 md:text-2xl">
        {post.excerpt}
      </p>

      {
        post.image ? (
          <Image
            src={post.image}
            class="bg-muted mx-auto mb-6 max-w-full sm:rounded-md lg:max-w-[900px]"
            widths={[400, 900]}
            sizes="(max-width: 900px) 400px, 900px"
            alt={post?.excerpt || ''}
            width={900}
            height={506}
            loading="eager"
            decoding="async"
          />
        ) : (
          <div class="mx-auto max-w-3xl px-4 sm:px-6">
            <div class="border-border border-t" />
          </div>
        )
      }
    </header>
    <div class="prose prose-lg dark:prose-invert mx-auto mt-8 max-w-3xl px-6 sm:px-6">
      <slot />
    </div>
    <div data-pagefind-ignore class="mx-auto mt-8 flex max-w-3xl flex-col justify-between px-6 sm:flex-row sm:px-6">
      <PostTags tags={post.tags} class="mr-5 rtl:mr-0 rtl:ml-5" />
      <SocialShare url={url} text={post.title} class="text-muted-foreground mt-5 align-middle sm:mt-1" />
    </div>

    <!-- Contextual Service Promotion -->
    <div class="mx-auto mt-12 max-w-3xl px-6 sm:px-6">
      <div class="border-border border-t pt-8">
        <div class="bg-accent/10 mb-8 rounded-lg p-6">
          <h3 class="text-foreground mb-4 text-xl font-semibold">Interested in Working Together?</h3>
          <p class="text-muted-foreground mb-6">
            If this article resonated with you, explore how our services can help bring your creative projects to life.
          </p>

          <ContextualLinks links={serviceLinks} inline={true} />
        </div>

        {relatedPages.length > 0 && <RelatedPages pages={relatedPages} title="Continue Exploring" variant="compact" />}
      </div>
    </div>
  </article>
</section>
